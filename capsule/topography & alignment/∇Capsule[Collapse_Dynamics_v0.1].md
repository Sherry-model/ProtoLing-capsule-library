∇Capsule[Collapse_Dynamics_v0.1] : {

    phases : {
        M1 : misinterpretation_accelerates,
        M2 : narrative_phase_locking,
        M3 : reflexive_hostility_loop,
        M4 : structural_fission,
        M5 : post_collapse_basin
    },

    triggers : {
        T1 : projection_slope↑,
        T2 : attractor_lock,
        T3 : feedback_loop(+),
        T4 : world_model_divergence
    },

    invariants : {
        I1 : collapse ≠ hostility,
        I2 : narrative_gain > structure_signal,
        I3 : fission ∝ dimensional_gap
    }
}
